{% extends "layout.njk" %}
{% block content %}
<main class="container">
    <div style="display: flex; justify-content: center" >
        <h1>{{title}}</h1>
        <img src="/sword_shield.png" alt="wow" style="width: 3rem; height: auto;">
    </div>
    <ul>
        {% for attack in weapon %}
            <li style="font-size: 1.5rem;" >{{attack.attack_name}}: {{attack.attack_description}}, mv {{attack.mv}}</li>
        {% endfor %}
    </ul>


</main>
{% endblock %}